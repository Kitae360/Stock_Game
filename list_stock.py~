import csv
reader = csv.reader(open('companylist.csv'))

existing_stocks = {}
for row in reader:
    key = row[0]
    if key in existing_stocks:
        pass
    existing_stocks[key] = row[1:]



lll = {'a': 0, "b": 0, 'c': 1}
for name, quantity in lll.items():
    if 0 == v:
        lll.pop(name, quantity)

print lll


